import { Palette } from "../palette.slint";

export component HorizontalDivider inherits Rectangle {
    background: Palette.neutral-light;
    width: 100%;
    height: 1px;
}
export component VerticallDivider inherits Rectangle {
    background: Palette.neutral-light;
    height: 100%;
    width: 1px;
}
